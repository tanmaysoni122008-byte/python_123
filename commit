# Python Project

A simple collection of Python scripts for basic operations.

## Files

* **`square.py`** - Calculates the square of a number.
* **`addition.py`** - Adds two numbers.
* **`name.py`** - Prints a name.
| Claim in README | True? | Evidence / Correction |
| --- | --- | --- |
| `square.py` calculates square | Yes | `square.py` contains square calculation logic. *(Note: fixed typo "sqyare" from original claim)* |
| `addition.py` adds two numbers | Yes | `addition.py` contains addition logic |
| `name.py` prints a name | Yes | `name.py` prints the entered name |
